import { useState } from 'react'
import { ArrowLeft } from 'lucide-react'
import { useLocalized } from '../../hooks/useLocalized'
export function NewsletterSection() { const [email, setEmail] = useState(''); const [submitted, setSubmitted] = useState(false); const { t, isArabic } = useLocalized(); return <section className="border-t border-line bg-cream dark:border-line-dark dark:bg-cream-dark"><div className="mx-auto max-w-[1440px] px-5 py-20 md:px-10 md:py-28"><div className="mx-auto max-w-xl text-center"><span className="text-xs font-semibold tracking-[.2em] text-gold">RAFIQ NOTES</span><h2 className="mt-4 font-ar-heading text-3xl font-semibold text-ink dark:text-ink-dark md:text-4xl">{t('خليك قريب من رفيق.', 'Stay close to Rafiq.')}</h2><p className="mt-4 text-base leading-8 text-muted dark:text-muted-dark">{t('أفكار جديدة، منتجات جديدة، وأشياء تساعدك تخلي مساحتك أفضل.', 'New ideas, thoughtful products and things that make your space feel better.')}</p>{submitted ? <p className="mt-8 font-medium text-burgundy">{t('تم الاشتراك بنجاح — أهلاً بك في رفيق.', 'You are subscribed — welcome to Rafiq.')}</p> : <form onSubmit={event => { event.preventDefault(); if (email) setSubmitted(true) }} className="mx-auto mt-8 flex max-w-md flex-col gap-3 sm:flex-row"><input value={email} onChange={event => setEmail(event.target.value)} type="email" required placeholder={t('بريدك الإلكتروني', 'Your email address')} className="min-w-0 flex-1 rounded-full border border-line bg-paper px-5 py-3.5 text-sm text-ink outline-none transition focus:border-burgundy dark:border-line-dark dark:bg-paper-dark dark:text-ink-dark" /><button className="inline-flex items-center justify-center gap-2 rounded-full bg-burgundy px-7 py-3.5 text-sm font-semibold text-cream transition hover:bg-burgundy-dark">{t('اشترك', 'Subscribe')}<ArrowLeft size={15} className={isArabic ? '' : 'rotate-180'} /></button></form>}</div></div></section> }
