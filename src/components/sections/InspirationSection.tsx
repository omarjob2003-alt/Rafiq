import { motion } from 'framer-motion'
import { SectionHeading } from '../ui/SectionHeading'
import { inspirationImages } from '../../data/content'
import { useLocalized } from '../../hooks/useLocalized'
import { cn } from '../../lib/cn'
const sizes: Record<string, string> = { tall: 'row-span-2 aspect-[3/4]', wide: 'col-span-2 aspect-[16/10]', square: 'aspect-square' }
const names: Record<string, string> = { 'مكتب صغير': 'Small desk', 'مكتب منزلي': 'Home office', 'للدراسة': 'For study', 'للمبدعين': 'For creators', Minimal: 'Minimal' }
export function InspirationSection() { const { t, isArabic } = useLocalized(); return <section id="inspiration" className="border-t border-line bg-cream dark:border-line-dark dark:bg-cream-dark"><div className="mx-auto max-w-[1440px] px-5 py-20 md:px-10 md:py-28"><SectionHeading eyebrow={t('مصادر إلهام', 'INSPIRATION')} heading={t('مساحات تلهمك', 'Spaces to inspire you')} subtitle={t('شوف رفيق في مساحات حقيقية.', 'See Rafiq in real, lived-in spaces.')} /><div className="mt-11 grid auto-rows-[160px] grid-cols-2 gap-3 md:auto-rows-[200px] md:grid-cols-3 md:gap-4">{inspirationImages.map((item, index) => <motion.div key={item.id} initial={{ opacity: 0, y: 15 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .05 }} className={cn('group relative overflow-hidden rounded-xl', sizes[item.size])}><img src={item.image} alt={isArabic ? item.category : names[item.category]} className="size-full object-cover transition duration-700 group-hover:scale-[1.06]" /><div className="absolute inset-0 bg-gradient-to-t from-ink/65 via-transparent opacity-0 transition group-hover:opacity-100" /><span className="absolute bottom-3 right-3 text-xs font-medium text-cream opacity-0 transition group-hover:opacity-100">{isArabic ? item.category : names[item.category]}</span></motion.div>)}</div></div></section> }
